<Window x:Class="курс.Menu"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:курс"
        mc:Ignorable="d"
        Title="Menu" Height="450" Width="800">
    <Grid>
        <ListBox HorizontalAlignment="Left" Height="286" Margin="134,109,0,0" VerticalAlignment="Top" Width="648">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel  Height="100" Width="100" Orientation="Horizontal">
                        <StackPanel Orientation="Vertical" Width="300">
                            <TextBlock FontWeight="Bold" Margin="10" FontSize="16">
                               
                       <Run Text="{Binding Zakazi.Data_prin_zak}"/>
                            </TextBlock>

                        </StackPanel>

                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

    </Grid>
</Window>
